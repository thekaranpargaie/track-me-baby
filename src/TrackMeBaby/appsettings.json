{
  "//": "Framework and infrastructure only. Every Tracker setting lives in docker-compose.yml — see the YOUR CONFIGURATION block there. Defaults for anything left unset are the property initialisers in TrackerOptions.cs, so there is no second place to keep in sync.",

  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "Kestrel": {
    "Endpoints": {
      "Http": {
        "Url": "http://127.0.0.1:5199"
      }
    }
  },
  "ConnectionStrings": {
    "//": "The host process talks to the Postgres container through its published port. Inside Docker this is overridden by ConnectionStrings__Tracker in docker-compose.yml.",
    "Tracker": "Host=localhost;Port=5433;Database=trackmebaby;Username=tracker;Password=tracker"
  }
}
